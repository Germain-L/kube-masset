apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  labels:
    app: gestion-produits
    variant: mysql
    tier: database
spec:
  selector:
    app: gestion-produits
    variant: mysql
    tier: database
  ports:
  - port: 3306
    targetPort: 3306
  clusterIP: None  # Headless service for StatefulSet
