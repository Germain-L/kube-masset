apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: gestion-produits-postgresql-ingress
  namespace: gestion-produits-postgresql
  annotations:
    ingressClassName: traefik
    cert-manager.io/cluster-issuer: "letsencrypt-prod"  # For certificate management
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  tls:
  - hosts:
    - gestion-produits-pg.germainleignel.com
    secretName: postgresql-tls-cert
  rules:
  - host: gestion-produits-pg.germainleignel.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-service-postgresql
            port:
              number: 80
